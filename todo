~, -, + arithmetic prefixes
Pratt parsing

loop over each property and try to lookup names, but input files need to be fully parsed??
oh and local #defines and labels too

re-enable lints with cst2

rename node to tree in RedNode, GreenNode, AstNode, etc.

use less Arc's? Or at least don't clone them when only using from a single thread?

use the lsp-server crate instead of tower_lsp

use bstr more

release VSCode extension

use FxHashMap
